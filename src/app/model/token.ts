export interface OathToken {
  refresh_token_expires_in: string;
  token_type: string;
  issued_at: string;
  client_id: string;
  access_token: string;
  expires_in: string;
  refresh_count: string;
}
